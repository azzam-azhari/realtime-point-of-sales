import Login from './_components/login';

export const metadata = {
  title: 'Karim Cafe | Login',
  description: 'Login to access your account on MyApp.',
};

export default function LoginPage() {
  return <Login />;
}
